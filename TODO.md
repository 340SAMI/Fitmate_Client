# TODO
- [x] Create a temporary `useAuth` hook so the Navbar can render without session/auth being set up.
- [ ] (Next) Fix HTML nesting warning: `header` rendered inside `html` in `src/app/layout.js`.

